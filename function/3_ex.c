// calculate the area of square with side
#include <stdio.h>
#include <math.h>
int main()
{
    int side;
    printf("the value of side\n");
    scanf("%d", &side);
    printf("the value of area is %f", pow(side, 2));
    return 0;
}